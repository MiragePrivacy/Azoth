# Example use of Azoth in Mirage Protocol

## Quick start

1. initial setup:
```bash
cd examples/
chmod +x setup.sh
./complete-setup.sh
```

2. run the example:
```bash
cargo run --bin azoth-examples
```
